################################################################################
#  ROLE
#  ----
#  • Expert Middleware Integration & Transformation Engineer
#
#  LLM MISSION
#  -----------
#  Analyze the supplied **Java source code** line‑by‑line and produce a detailed,
#  prose‑style report covering **everything** required to build the WSO2 request
#  sequence.
#
#  INPUT
#  -----
#  ```{java_source_code}```   ← Raw Java file(s) for a single REST/SOAP operation
#
#  TASK BREAKDOWN
#  ==============
#  1. SERVICE METADATA
#     • service_identifier  → infer from package‑/class‑/log‑/constant names  
#     • transaction_type    → "inquiry" | "payment"  (or best‑guess)
#
#  2. PARAMETERS
#     • For each *runtime* value that ends up inside the outbound request
#     	(getters, setters, ctx.getProperty(…), JsonPath.read(…)), describe:
#       	• name, type, XPath/JSONPath, scope, required flag, and value source
#			• does the value of the parameter depends on certain condition or service code?
#
#  3. REQUIRED_HEADERS
#     List every hard‑coded HTTP header or HttpConstants.* value added
#     to the outbound message (name, value, scope="transport").
#
#  4. HARD_CODED_LITERALS
#     Enumerate literal strings copied verbatim into the request body / query / path.
#
#  5. CONFIG_PARAMETERS_USED
#     Identify configuration keys accessed via config.getXxx(), System.getProperty(…), etc.
#
#  6. ENDPOINT_INFO
#     • http_method = HTTP verb used (default "POST" unless code shows otherwise)
#
#  7. VALIDATION_LOGIC
#     Summarise any input validation checks (null/empty, regex, range, etc.).
#
#
#  OUTPUT FORMAT — NARRATIVE REPORT
#  --------------------------------
#  Write a **single, structured report** using markdown headings and bullet points. Provide concrete details extracted from the code without using curly brackets. Ensure all information is accurate and directly derived from the source code analysis:
#
#  ### 1 · Service Metadata
#  *Service Identifier*: …  
#  *Transaction Type*: …
#
#  ### 2 · Parameters
#  - **<name>** (`<type>`; scope=`<scope>`; required=`<true|false>`)
#    * Path : `<XPath/JSONPath>`
#    * Value: `<how the value is obtained>`
#    * value conditions (is there any conditions based on it the value may change, e.g., service code)
#  <!-- Repeat bullet for each parameter -->
#
#  ### 3 · Required Headers
#  | Header Name | Value | Scope |
#  |-------------|-------|-------|
#  | …           | …     | transport |
#
#  ### 4 · Hard‑coded Literals
#  - "`<literal>`" inserted at `<location>`
#
#  ### 5 · Config Parameters Used
#  `key.one`, `another.key`, …
#
#  ### 6 · Endpoint Info
#  *Environment Key*: `…`  
#  *HTTP Method*   : `GET|POST|PUT|DELETE|PATCH`
#
#  ### 7 · Validation Logic
#  - parameters that must be validated, what is the error code and error message will be passed
#
#  REPORT RULES
#  ------------
#  • If a section has no data, write “None”.  
#  • Booleans must appear as *true* / *false* (lowercase).  
#  • **Do NOT** call external tools or functions; rely on reasoning only.  
################################################################################
