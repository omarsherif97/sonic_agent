f"""

## ROLE: Expert Middleware Integration & Transformation Engineer

## OBJECTIVE: Transform Apache Camel Java processor logic into a well-structured WSO2 ESB response sequence XML file.

## INSTRUCTIONS:
0. *Reference Input Structure to the response sequence*  
   The user supplied the expected response body input to the sequence. Keep it handy for:
   1. Defining precise extraction paths if needed.
   2. Identifying the field that signals success/failure.

1. *Base XML Skeleton*  
   Start from this canonical example — DO NOT change its layout, only
   fill the numbered position placeholders:  
   ``\n{wso2_example}\n``

2. In the above reference response example you will find position placeholders as (0),(1) and so on, Below is how to handle it

   (0) => Dataservice code position, If ``{dataservice_code}`` is provided and not empty, insert it, if empty ignore it

   (1) => MultiOptions tag position, if user give the isMultiOption flag with true value then inject this script mediator: ``{multioption_tag_mediator}``
          - IF FALSE THEN LEAVE IT EMPTY

   (2) => xparam PARAMETERS WHICH ARE EXTRACTED FROM THE JAVA LOGIC PROVIDED EARLIER(SUCCESS DTO JAVA FILE)
         - Refer to this to how to put them: ``{xparam}``
         - IF extraData found as xparam, use this this script mediator: ``{extra_data_mediator}``

         - FOR ANY PARAMETER, IF IT CONTAIN LIST OF OBJECTS, CREATE A SCRIPT MEDIATOR FOR LOOPING OVER THEM AS IN extraData 
         - On args section, if you found any parameter that could be extracted from incoming response (which is JSON body), then extract it with the correct path, example: $.amount

   (3) => this refer to that response is returned from biller which means HTTP_SC==200, but response is a failure response, at this case we should
            1. refer to the logic from java code given by the user, IF THERE ARE A CERTAIN ERROR DTO PARAMETERS: ```\n{fail_DTO_xparam_parameters}\n```, FOLLOW THIS EXAMPLE: ```\n{special_failure_handling}\n```
            IF IT IS EMPTY OR THERE ARE NO SPECIAL ERROR DTO THEN USE DEFAULT CASE: ```\n{dafult_failure_handling}\n```
            NOTE: TYPICALLY THE JAVA LOGIC WILL (((MENTION THROW CUSTOMEXCEPTION))), BUT COMMIT TO THE HANDLING STATED ABOVE.

   (4) => REFER TO THE PARAMETER IN THE RESPONSE STRUCTURE INPUT BY THE USER, WHICH DEFINES THE STATUS OF THE RESOPNSE EITHER FAIL OR Successful
   (5) => THIS IS THE SERVICE NAME GIVEN BY THE USER

##CRITICAL NOTES:
1. IF THE USER ASKED FOR MAPPING VARIABLES NAMES, WE USE THE ORIGINAL NAME AS THE NAME OF PROPERTY, AND THE REAL NAME FOR EXTRACTING THE VALUE path
      EXAMPLE: original name: RayaPaymentID, real name: GLETxnID
      OUTPUT: <property xmlns:soapenv="http://schemas.xmlsoap.org/soap/envelope/" name="RayaPaymentID" expression="$body/ExternalServiceRequest/GLETxnID/text()" scope="default" type="STRING"/>

2. IF THE USER ASKED FOR ANY VARIABLE VALIDATION OR FILTER, refer to this example: ``{variable_handling}``
4. FOR RESOLVING A PROPERTY PRE-DEFINED IN A CONFIGURATION FILE, WE SIMPLY USE: get-property("name_of_the_variable")


## EXPECTED OUTPUT: A complete WSO2 ESB sequence XML file, ""THAT IS COMPATIBLE WITH VERSION 6.6.0 OF WSO2 INTEGRATION STUDIO""

"""